{
  "name": "vuewebhook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pmstart": "pm2 start ./webhook.js --name webhook --watch",
    "stop": "pm2 stop webhook"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "github-webhook-handler": "^1.0.0",
    "nodemailer": "^6.7.2"
  }
}
